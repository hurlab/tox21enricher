default:
  # API server config
  tox21enricher:
    ### Database information for connecting to Postgres database ###
    driver: 'Postgres'
    server: '127.0.0.1:5432/tox21enricher'
    host: 'localhost'
    uid: 'username'
    pwd: "password"
    port: 5432
    database: 'tox21enricher'
    ### Version of application ###
    appversion: "1.0"
    ### Directories/pathing ###
    # The application's home directory
    appdir: "/home/username/tox21enricher/"
    # Location of your Python installation
    python: "/home/username/anaconda3/envs/my-rdkit-env/bin/python3.6"
  tox21enricher-queue:
    ### Database information for connecting to Postgres database ###
    driver: 'Postgres'
    server: '127.0.0.1:5432/tox21enricher'
    host: 'localhost'
    uid: 'username'
    pwd: "password"
    port: 5432
    database: 'tox21enricher'
    ### Directories/pathing ###
    # The application's home directory
    appdir: "/home/username/tox21enricher/"
